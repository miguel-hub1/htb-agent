services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    volumes:
      - ollama_data:/root/.ollama
    ports:
      - "11434:11434"
    networks:
      - htb-network

    # ✅ GPU EN WINDOWS
    gpus: all

  agent:
    build:
      context: .
      dockerfile: Dockerfile.agent
    container_name: htb-agent
    depends_on:
      - ollama
    volumes:
      - ./agent:/app
      - ./results:/results
    environment:
      - OLLAMA_HOST=http://ollama:11434
    networks:
      - htb-network
    cap_add:
      - NET_ADMIN
      - NET_RAW
    stdin_open: true
    tty: true
    command: tail -f /dev/null

volumes:
  ollama_data:

networks:
  htb-network:
    driver: bridge
